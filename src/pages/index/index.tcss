.banner {
  width: 100%;
  height: 100px;
}
.container {
  display: flex;
  flex-wrap: wrap;
  padding: 0 12px;
}
.item {
  width: calc(50% - 8px);
  margin: 4px;
}
.thumb {
  width: 100%;
  height: 150px;
}